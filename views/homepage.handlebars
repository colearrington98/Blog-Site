{{#each blogs as /blog/}}
<div class = "container">
    <div class = "row">
        <div class = "col-md-12">
            <h1>{{blog.title}}</h1>
            <p>{{blog.body}}</p>
        </div>
    </div>